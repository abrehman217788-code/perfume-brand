<%- include('partials/header') %>

<main class="flex-grow flex items-center justify-center p-8">
    <div class="border-[3px] border-on-background p-12 text-center max-w-lg bg-surface relative overflow-hidden reveal">
        <div class="absolute top-0 left-0 w-full h-2 bg-tertiary"></div>
        <h1 class="text-4xl font-black uppercase tracking-tighter mb-4 text-on-background">ORDER CONFIRMED</h1>
        <p class="font-label-mono text-gray-400 mb-8">Your fragments have been secured. We will notify you when they ship.</p>
        <div class="font-label-mono border-y-2 border-dashed border-gray-600 py-4 mb-8 text-sm text-left">
            <p>ORDER STATUS: <span class="text-tertiary">PROCESSING</span></p>
            <p>PAYMENT: <span class="text-on-background">BYPASSED (OFFLINE MODE)</span></p>
        </div>
        <a href="/shop" class="inline-block bg-white text-black px-8 py-4 font-black uppercase tracking-widest hover:bg-tertiary hover:text-black transition-colors w-full dark:bg-white dark:text-black">RETURN TO LAB</a>
    </div>
</main>

<%- include('partials/footer') %>
